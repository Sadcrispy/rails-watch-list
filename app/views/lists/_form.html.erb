<div class="card-category flex-column p-2" style="background-image: url()">
  Add a list
  <%= simple_form_for @list do |f| %>
    <%= f.input :name, label: false %>
    <%= f.input :photo, as: :file, label: false %>
    <%= f.submit class: "btn btn-flat" %>
  <% end %>
</div>
